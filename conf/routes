# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
POST    /api/v1/:pkg/:step                      controllers.Controller.routes(pkg: String, step: String)
POST    /upload                           controllers.Controller.upload()
